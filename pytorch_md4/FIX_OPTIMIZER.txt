# Fix for using list of optimizers in train.py

# Line 69-71: Replace
#   optimizer.zero_grad()
#   loss.backward()
#   optimizer.step()
# With:
#   for opt in optimizer:
#       opt.zero_grad()
#   loss.backward()
#   for opt in optimizer:
#       opt.step()

# Line 103: Replace
#   "train/learning_rate": optimizer.param_groups[0]["lr"],
# With:
#   "train/learning_rate": optimizer[0].param_groups[0]["lr"],
